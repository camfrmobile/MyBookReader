# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyBookReader' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyBookReader

	pod 'Alamofire'
	pod 'SwiftSoup'
	pod 'SwiftyJSON', '~> 4.0'
	pod 'Kingfisher', '~> 7.0'
  pod 'Cosmos', '~> 23.0'
  
	pod 'FirebaseAuth'
  pod 'FirebaseFirestore'
  pod 'FirebaseStorage'
  
end
